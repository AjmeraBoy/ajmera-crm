import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ajmera Fashion CRM | In-House Business Management",
  description: "Unified CRM for Online & Export departments — Leads, Telecalling, WhatsApp, Quotations, Payments, Dispatch, Support & Analytics.",
  keywords: ["Ajmera Fashion", "CRM", "B2B", "Lead Management", "Export", "Textile"],
  authors: [{ name: "Ajmera Fashion Limited" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
